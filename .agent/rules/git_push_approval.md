# Git Push Policy Rule

- Always ask the user for explicit confirmation before running `git push` or pushing code changes to GitHub.
- Never execute `git push` automatically without prior user permission.
